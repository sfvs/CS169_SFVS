%h2= "Welcome #{@user.email}"

%p
	%strong name
%p
	%strong company
%p
	%strong Application type:
	-if @application
		="#{@application.application_type}"
	-else
		status




= link_to "Health Form", form_user_path(:type => "health_form")

%p payment

= button_to "Take Survey", survey_user_path, {:id => "questionnaire_button", method: :get}
= button_to "Edit Profile", edit_user_path(@user)
= button_to "Logout", destroy_user_session_path, {:id => "logout_but", method: :delete}