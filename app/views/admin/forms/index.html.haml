%h3 Forms List

%div
  %table#forms
    %thead
      %tr
        %th Form
        %th Edit
    %tbody
      - if !@forms.nil?
        - @forms.each do |form|
          %tr
            %td= form.form_name
            %td= link_to "Edit", edit_admin_form_path(form)

= button_to "Create Form", new_admin_form_path, {:id => "create_but", method: :get}