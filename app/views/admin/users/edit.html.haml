%body

  = render 'admin/navi_bar'

  %div.container-fluid

    %h1 Edit User

    = button_to "Delete User", admin_user_path(@user), {:class => "btn btn-danger", method: :delete, data: { confirm: "Are you sure?"}}

    = button_to "Back", admin_user_path(@user), {:class => "btn btn-default", method: :get}